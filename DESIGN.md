# DESIGN.md — 金鹰世界物业小程序设计系统

> 基于 Apple 设计体系（references/apple.md），适配微信小程序 rpx 单位。
> 所有页面样式必须遵循本文件，禁止引入体系外颜色与组件风格。

## 1. 视觉气质

克制、高级、留白即设计。界面退到内容后面：
- 纯色背景，禁止渐变/纹理/图案
- 唯一强调色 Apple Blue，只给可交互元素
- 标题紧凑（负字距 + 紧行高），正文舒展
- 卡片无描边，层级靠背景色对比，阴影极少量

## 2. 配色（wxss 直接可用）

### 基础
| 角色 | 值 | 用途 |
|---|---|---|
| 页面背景 | `#f5f5f7` | 全局 page 背景（浅蓝灰，非纯白） |
| 纯黑 | `#000000` | 仅功能性场景：相机取景、视频播放器 |
| 卡片面 | `#ffffff` | 卡片、输入区、弹层 |
| 主文字 | `#1d1d1f` | 标题、正文 |
| 次文字 | `#6e6e73` | 描述、辅助信息 |
| 弱文字 | `#86868b` | 占位、时间戳、脚注 |
| 分割线 | `#d2d2d7` | 极细分隔（0.5px），能不用就不用 |

### 交互
| 角色 | 值 | 用途 |
|---|---|---|
| 主按钮蓝 | `#0071e3` | 主要 CTA 填充 |
| 链接蓝 | `#0066cc` | 文字链接、次级操作 |
| 按压态 | `#005bb8` | 主按钮 active |
| 浅蓝底 | `#e8f0fe` | 选中态背景、标签底（慎用） |

### 语义（Apple 系统色）
| 角色 | 值 |
|---|---|
| 成功 | `#34c759` |
| 警告 | `#ff9500` |
| 危险 | `#ff3b30` |

### 功能图标色板（iOS 设置风格，仅限图标 chip）
功能入口图标 = 彩色圆角方块 + 白色线性图标，像 iOS 设置一样用颜色区分功能：
| 色 | 值 | 建议用途 |
|---|---|---|
| 蓝 | `#0071e3` | 问答/核心功能 |
| 橙 | `#ff9500` | 报修/工具 |
| 绿 | `#34c759` | 记录/工单 |
| 红 | `#ff3b30` | 培训/学习 |
| 紫 | `#af52de` | 考试/成就 |
| 青 | `#5ac8fa` | 个人/账户 |

纪律：彩色只出现在 64-112rpx 的小图标容器上；大面积背景、按钮仍遵守"唯一强调色"。chip 圆角取边长 28%（96rpx 方块 → 28rpx 圆角），图标纯白。

### 深色面（仅限功能性场景）
- **纯黑 `#000000` 只用于功能性场景**：相机取景器、照片预览、视频播放器底色。禁止用于装饰性 Hero/头部等大区块
- 深色功能场景内：文字 `#ffffff`，次要文字 `rgba(255,255,255,0.6)`

### Hero / 页首区块
- 一律浅色：背景 `#f5f5f7`（与页面融为一体）或 `#ffffff`
- 标题 `#1d1d1f`（负字距紧凑排版），副标题 `#6e6e73`
- 靠大留白和排版制造高级感，不靠底色反差

## 3. 字体

```
font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'PingFang SC', 'Helvetica Neue', Helvetica, Arial, sans-serif;
```

| 层级 | 大小(rpx) | 字重 | 行高 | 字距 |
|---|---|---|---|---|
| Hero 大标题 | 64 | 600 | 1.1 | -1rpx |
| 页面大标题 | 48 | 600 | 1.15 | -0.5rpx |
| 区块标题 | 36 | 600 | 1.2 | 0 |
| 卡片标题 | 32 | 600 | 1.25 | 0 |
| 正文 | 30 | 400 | 1.5 | 0 |
| 正文强调 | 30 | 600 | 1.3 | 0 |
| 辅助说明 | 26 | 400 | 1.35 | 0 |
| 脚注/标签 | 22 | 400 | 1.35 | 0 |

规则：字重只用 400/600，禁 700+；正文/标题禁正字距。**唯一例外：eyebrow 眉题标签**（22rpx 小标签，如英文类目、日期），用 +3~4rpx 正字距制造编辑感。

### 排版节奏（防"AI 味"）
- **左对齐编辑式**：页首用"眉题(22rpx/+3rpx/#86868b) + 大标题(56-64rpx/600/-1rpx) + 一句话副标题(26rpx/#6e6e73)"三段式，左对齐，不要满屏居中
- **组件形态要有对比**：同页至少两种容器形态（如"特色大卡 + 分组列表"），禁止清一色同款卡片平铺
- **分组列表（inset grouped list）**：iOS 风格——一个白圆角容器装多行，行间 0.5px `#d2d2d7` 细线（左侧缩进对齐文字），行尾 chevron `›` 用 `#c7c7cc`；列表类页面优先用它，而不是 N 张悬浮卡

## 4. 组件规范

### 按钮
- **主按钮**：背景 `#0071e3`，白字，圆角 `980rpx`（胶囊），高度 96rpx，字号 32rpx，active 时 `#005bb8`
- **深色按钮**：背景 `#1d1d1f`，白字，胶囊
- **描边按钮**：透明底，`#0066cc` 文字 + 1rpx `#0066cc` 描边，胶囊
- **禁用**：背景 `#e8e8ed`，文字 `#86868b`
- 按钮无阴影

### 卡片
- 白底 `#ffffff`，圆角 `24rpx`，**无描边**
- 默认无阴影；仅浮层/重点卡用 `0 6rpx 60rpx rgba(0,0,0,0.12)`
- 内边距 32-40rpx

### 输入
- 浅灰底 `#f5f5f7` 或白底 + 0.5px `#d2d2d7`，圆角 20rpx
- focus 描边 `#0071e3`

### 标签/状态徽标
- 圆角胶囊，字号 22rpx，底色用语义色 10% 透明度，文字用语义色

### Hero 品牌区
- 浅色底（`#f5f5f7` 或 `#ffffff`），深色紧凑标题（负字距），副标题 `#6e6e73`
- 禁渐变、禁大面积纯黑

## 5. 布局

- 页面左右边距 32rpx
- 卡片间距 24rpx
- 区块间距 48rpx
- 圆角刻度：20rpx（输入/小元素）/ 24rpx（卡片）/ 980rpx（胶囊按钮、标签）
- 导航栏：小程序自定义导航用白底或黑底，勿用彩色

## 5.5 动效

只用 transform 和 opacity（性能），时长克制，曲线统一：

- **曲线**：标准 `cubic-bezier(0.2, 0, 0, 1)`；弹性按压 `cubic-bezier(0.34, 1.56, 0.64, 1)`
- **时长**：微交互 150-250ms；入场 400-500ms；循环动画 ≥1.2s（不急躁）
- **入场编排**：页面元素交错淡入上移（fadeUp：opacity 0→1 + translateY 24rpx→0），相邻元素延迟 60-80ms，从页首到页尾依次展开
- **按压反馈**：卡片/列表行 active 时 `scale(0.96-0.98)` + 弹性曲线；禁用透明度突变当反馈
- **加载态**：用骨架屏（灰块 + shimmer 扫光动画），禁用干文字"加载中..."
- **禁止**：旋转/弹跳/闪烁类廉价动画；同时超过 3 个循环动画；动画改变布局（width/height/margin）

## 5.6 层级与质感

- 层级靠"背景色对比 + 极少量阴影"制造，不靠描边
- 重点卡（页面主角，每页至多 2 个）允许 `0 8rpx 32rpx rgba(0,0,0,0.06)` 微阴影浮起；其余容器平贴背景
- 同页容器体量要有节奏过渡：大卡 : 中行 : 列表行 ≈ 3 : 2 : 1，禁止悬殊反差

## 6. Do / Don't

**Do**
- Apple Blue 只给可交互元素
- 大留白，内容克制
- 状态色只用 3 个 Apple 系统色

**Don't**
- 禁渐变、纹理、图案背景
- 禁大面积纯黑装饰区块（Hero、页首、卡片）；纯黑仅限相机取景/视频播放等功能场景
- 禁卡片描边、多重阴影
- 禁引入体系外强调色（原来的 #3370ff 一律替换）
- 禁圆角超过 24rpx 的矩形元素（胶囊除外）
- 禁字重 700+
